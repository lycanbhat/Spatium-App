// KeyboardType
export enum KeyboardType {
  'default' = 'default',
  'email-address' = 'email-address',
  'phone-pad' = 'phone-pad',
}

// FieldType
export enum FieldType {
  'text',
  'otp',
}

// PassCardType
export enum PassCardType {
  'buildingPass' = 'Building Pass',
  'parkingPass' = 'Parking Pass',
}
